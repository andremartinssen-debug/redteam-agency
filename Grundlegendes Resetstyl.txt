/* Grundlegendes Reset */
* { margin:0; padding:0; box-sizing:border-box; }

body {
  font-family: 'Roboto', sans-serif;
  background-color: #121212;
  color: #ffffff;
  line-height: 1.6;
}

.container { width: 90%; max-width: 1200px; margin: auto; }

/* HEADER */
header {
  background-color: #111;
  padding: 15px 0;
  position: sticky;
  top:0;
  z-index:1000;
}

header .logo img { height: 50px; }

nav ul {
  display: flex;
  list-style: none;
  justify-content: flex-end;
}

nav ul li { margin-left: 30px; }

nav ul li a {
  text-decoration: none;
  color: #ffffff;
  font-weight: 500;
}

nav ul li a:hover { color: #1e90ff; }

/* HERO */
#hero {
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  height: 80vh;
  background: #1a1a1a url('https://images.unsplash.com/photo-1556741533-f6acd647d2fb?auto=format&fit=crop&w=1950&q=80') no-repeat center/cover;
}

#hero h1 { font-size: 3em; margin-bottom: 20px; color: #1e90ff; }
#hero p { font-size: 1.2em; margin-bottom: 30px; }
.btn {
  text-decoration: none;
  background-color: #1e90ff;
  color: #fff;
  padding: 12px 25px;
  border-radius: 5px;
}

/* SERVICES */
#services { padding: 60px 0; }
#services h2 { text-align:center; margin-bottom:40px; color:#1e90ff; }
.service-grid { display:flex; flex-wrap:wrap; gap:30px; justify-content:center; }
.service-item { background:#1f1f1f; padding:30px; border-radius:10px; width:300px; text-align:center; }
.service-item h3 { margin-top:15px; margin-bottom:10px; color:#1e90ff; }
.service-item p { font-size:0.95em; }

/* CONTACT */
#contact { padding:60px 0; background:#111; text-align:center; }
#contact a { color:#1e90ff; text-decoration:none; }
#contact a:hover { text-decoration:underline; }

/* FOOTER */
footer { text-align:center; padding:20px 0; background:#0d0d0d; font-size:0.9em; }

/* Mobile responsive */
@media(max-width:768px){
  nav ul { flex-direction:column; align-items:center; }
  nav ul li { margin:10px 0; }
  .service-grid { flex-direction:column; align-items:center; }
}
